#!/bin/sh
x=$(echo -e "browser\nfiles\nhtop\nvirtualbox" | dmenu -i -p "Favorites")

case "$x" in
    browser) qutebrowser & ;;
    files) st vifm & ;;
    htop) st htop & ;;
    virtualbox) virtualbox & ;;
esac
